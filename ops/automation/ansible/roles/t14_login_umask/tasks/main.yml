---
- name: Set default UMASK for login sessions (best-effort)
  when: lab-node-01_login_umask_enable | bool
  ansible.builtin.lineinfile:
    path: /etc/login.defs
    regexp: '^\s*UMASK\s+'
    line: "UMASK\t{{ lab-node-01_login_umask }}"
    create: false
  failed_when: false

