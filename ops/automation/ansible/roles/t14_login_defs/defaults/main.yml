---
# /etc/login.defs — umask + password aging + hash parameters (LAB-OP defaults).
t14_login_defs_enable: true

t14_login_umask: "027"

# HOME_MODE for new home dirs (useradd/newusers).
t14_login_defs_home_mode: "0750"

# Password aging (shadow/login.defs).
t14_login_defs_pass_max_days: "99990"
t14_login_defs_pass_min_days: "1"
t14_login_defs_pass_warn_age: "14"

# Fallback if PAM does not enforce (see login.defs(5)).
t14_login_defs_login_retries: "5"

# Hash algorithm for new/changed passwords (must align with PAM if you change it).
t14_login_defs_encrypt_method: "SHA512"

# Stretching for SHA256/SHA512 crypt (libc range).
t14_login_defs_sha_crypt_min_rounds: "6000"
t14_login_defs_sha_crypt_max_rounds: "60000"
