---
- name: Skip shell umask snippets
  when: not (lab-node-01_shell_umask_enable | bool)
  ansible.builtin.debug:
    msg: "Shell umask snippets disabled (lab-node-01_shell_umask_enable=false)."

- name: Ensure system-wide umask in /etc/profile and bash.bashrc
  when: lab-node-01_shell_umask_enable | bool
  ansible.builtin.blockinfile:
    path: "{{ item }}"
    marker: "# {mark} ANSIBLE MANAGED BLOCK lab-node-01_shell_umask"
    block: |
      umask {{ lab-node-01_shell_umask }}
    create: false
  loop:
    - /etc/profile
    - /etc/bash.bashrc
