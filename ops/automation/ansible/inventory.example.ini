[t14]
example-host ansible_host=CHANGEME ansible_user=CHANGEME

# Optional — set false to skip Docker CE (default in t14-baseline.yml is true).
# [t14:vars]
# t14_install_docker_ce=false

[all:vars]
ansible_python_interpreter=/usr/bin/python3
