# Operator social posting windows — placeholder (tracked)

**Not the real policy.** Copy the pattern to **`docs/private/social_drafts/OPERATOR_SOCIAL_POSTING_WINDOWS.pt_BR.md`** on your machine and edit there.

**Why private only:** Detailed schedules (clock times, home-office separation) are **operator context** and must **not** be committed to GitHub `origin`. The whole **`docs/private/`** tree is gitignored from the public repo; version it in the **nested private git** if you use one.

**Public repo:** keep at most a **one-line** pointer in tracked operator docs (e.g. “see gitignored `docs/private/...`”) — no hour tables.

See also **[README.md](README.md)** in this folder.
