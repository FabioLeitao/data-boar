# Employers evidence hub — placeholder (tracked)

## Private-only content

Real layout and hashes live only under gitignored `docs/private/employers/`.

- **Prior employers (pre-Corporate-Entity-A):** optional hubs such as `cnseg_fenaseg/`, `wings/`, `onsite/`, `appi/`, `muxi/` — see private `PRIOR_EMPLOYERS_ROUTING.pt_BR.md`.
- **Integrity:** hash manifests (SHA-256 / SHA-512) + optional GPG detached signatures per employer folder.
- **Public script:** `scripts/evidence-hash-manifest.ps1` — generic; pass paths locally; do not commit private PDFs to `origin`.

See **`docs/PRIVATE_OPERATOR_NOTES.md`** and private **`employers/README.pt_BR.md`** after copying the tree locally.
