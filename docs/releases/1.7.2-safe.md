# Release 1.7.2+safe / tag v1.7.2-safe (Golden — Clean Slate)

**Status:** **Published** — Git tag **`v1.7.2-safe`**, GitHub Release, Docker Hub **`fabioleitao/data_boar:v1.7.2-safe`** + **`latest`** (same digest).

**Version strings (read this once):**

| Surface | Value | Notes |
| --- | --- | --- |
| **`pyproject.toml` / PyPI / `importlib.metadata`** | **`1.7.2+safe`** | Valid **PEP 440** (local segment `safe`). |
| **Git tag / Docker Hub marketing tag** | **`v1.7.2-safe`** | Operator-facing **immutable** pull string. |

Behaviour and features are the **1.7.1** codebase line plus subsequent `main` work through the hygiene publish; this release is the **sanitized distribution** milestone — not a large feature delta vs **1.7.1**.

---

## Security & Sanitization (why “Golden”)

- **Public Git history:** Prior **PII / sensitive-history remediation** (filter-class work) is **complete** on the default branch narrative operators should trust for **clones and audits**.
- **Docker Hub:** **Deprecated** all **prior `fabioleitao/data_boar` image tags** in favour of **`v1.7.2-safe`** and **`latest`**, both pointing at the **same slim digest** (tight **`.dockerignore`**, no multi‑GB context drift). **Do not** document or pin **removed** Hub tags — see [DOCKER_HUB_REPOSITORY_DESCRIPTION.md](../ops/DOCKER_HUB_REPOSITORY_DESCRIPTION.md).
- **Supply Colleague-Nn posture:** Rebuild from audited tree; align **Scout** / digest pinning with [DOCKER_IMAGE_RELEASE_ORDER.md](../ops/DOCKER_IMAGE_RELEASE_ORDER.md).

---

## Bump type

| From | To | Type |
| --- | --- | --- |
| **1.7.1** | **1.7.2+safe** | **Security & sanitization** publish (golden line) |

---

## How to get 1.7.2+safe / v1.7.2-safe

- **From source:** `uv sync` at **`v1.7.2-safe`** (or **`main`** when it matches this tag).
- **Docker:** `docker pull fabioleitao/data_boar:v1.7.2-safe` or `docker pull fabioleitao/data_boar:latest`.

---

## Publish checklist (operator — already done for this drop)

1. **GitHub:** Release **`v1.7.2-safe`** points at the **`1.7.2+safe`** `pyproject.toml` commit.
1. **Docker Hub:** **`v1.7.2-safe`** + **`latest`**; **repository description** pasted from **`docs/ops/DOCKER_HUB_REPOSITORY_DESCRIPTION.md`**.
1. **README** (EN + pt-BR): **Current release** row updated.
1. **`docs/ops/today-mode/PUBLISHED_SYNC.md`** (+ pt-BR) reconciled with Hub digest.

---

## Optional

- `docker scout quickview fabioleitao/data_boar:v1.7.2-safe`
